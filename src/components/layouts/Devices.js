const Devices = (props) => {
  return (
    <div style={{ fontSize: "4rem", textAlign: "center", color: "red" }}>
      devices page to be implemented soon!
    </div>
  );
};

export default Devices;
